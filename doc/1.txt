1. admin/index.html 
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">

    <title>CineBrain Admin Dashboard - Real-time Analytics & System Monitoring</title>
    <!-- Security Headers - Updated CSP -->
    <meta http-equiv="Content-Security-Policy" content="
    default-src 'self';
    script-src 'self' 'unsafe-inline' https://cdn.jsdelivr.net https://fonts.googleapis.com https://fonts.gstatic.com;
    style-src 'self' 'unsafe-inline' https://cdn.jsdelivr.net https://fonts.googleapis.com;
    font-src 'self' https://fonts.gstatic.com;
    img-src 'self' data: blob: https: http:;
    connect-src 'self' https://cinebrain.onrender.com https://api.themoviedb.org https://api.jikan.moe https://localhost:5000 http://127.0.0.1:5000 ws://localhost:* wss://localhost:* ws://127.0.0.1:* wss://127.0.0.1:* ws://cinebrain.onrender.com wss://cinebrain.onrender.com https://cdn.jsdelivr.net;
    base-uri 'self';
    form-action 'self';
    object-src 'none';
  ">
    <!-- Meta Tags -->
    <meta name="description"
        content="CineBrain Admin Dashboard - Real-time analytics, content management, system monitoring and health checks">
    <meta name="robots" content="noindex, nofollow">
    <meta name="author" content="CineBrain">

    <!-- Favicons -->
    <link rel="icon" type="image/x-icon" href="/favicon.ico">
    <link rel="icon" type="image/png" sizes="32x32" href="/assets/images/favicon-32x32.png">
    <link rel="apple-touch-icon" sizes="180x180" href="/assets/images/apple-icon-180x180.png">

    <!-- Preconnects -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link rel="preconnect" href="https://cdn.jsdelivr.net" crossorigin>

    <!-- Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Bangers&family=Inter:wght@300;400;500;600;700&display=swap"
        rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Core CSS -->
    <link rel="stylesheet" href="/assets/css/components/theme.css">
    <link rel="stylesheet" href="/assets/css/components/topbar.css">
    <link rel="stylesheet" href="/assets/css/components/mobile-nav.css">
    <link rel="stylesheet" href="/assets/css/components/footer.css">

    <!-- Core Dashboard Styles -->
    <link rel="stylesheet" href="/assets/css/admin/main/dashboard.css">
    <link rel="stylesheet" href="/assets/css/admin/main/system.css">
    <link rel="stylesheet" href="/assets/css/admin/main/statistics.css">
    <link rel="stylesheet" href="/assets/css/admin/main/emailpreferences.css">

    <!-- Theme Initialization -->
    <script>
        (function () {
            try {
                const savedTheme = localStorage.getItem('cinebrain-theme');
                const theme = (savedTheme === 'light' || savedTheme === 'dark') ? savedTheme : 'dark';
                document.documentElement.setAttribute('data-theme', theme);
                document.documentElement.setAttribute('data-bs-theme', theme);
            } catch (e) {
                document.documentElement.setAttribute('data-theme', 'dark');
                document.documentElement.setAttribute('data-bs-theme', 'dark');
            }
        })();
    </script>
</head>

<body class="admin-dashboard">
    <!-- Skip Link -->
    <a href="#main-content" class="visually-hidden-focusable">Skip to main content</a>

    <!-- Loading Indicator -->
    <div id="page-loading-indicator" class="page-loading-indicator"></div>

    <header>
        <nav class="navbar navbar-expand-lg navbar-cinebrain fixed-top" role="navigation" aria-label="Main navigation">
            <div class="container-fluid">
                <div class="navbar-brand-wrapper">
                    <a href="/" style="text-decoration: none;" aria-label="CineBrain Admin Home">
                        <h1 class="navbar-brand-cinebrain">CineBrain</h1>
                        <div class="tagline">
                            <span class="tagline-full d-none d-md-block">Admin Dashboard</span>
                            <span class="tagline-medium d-none d-sm-block d-md-none">Admin Panel</span>
                            <span class="tagline-short d-sm-none">Admin</span>
                        </div>
                    </a>
                </div>

                <div class="search-container desktop-search d-none d-lg-block" id="desktopSearchContainer">
                    <div class="search-wrapper">
                        <div class="search-bar-wrapper">
                            <div class="search-input-wrapper">
                                <i data-feather="search" class="search-icon" aria-hidden="true"></i>
                                <input type="text" class="form-control search-input" id="desktopSearchInput"
                                    placeholder="Search content, users, tickets..." autocomplete="off"
                                    aria-label="Search admin content" maxlength="100">
                                <button class="search-clear" id="searchClear" aria-label="Clear search"
                                    style="display: none;">
                                    <i data-feather="x" aria-hidden="true"></i>
                                </button>
                            </div>
                            <div class="search-results" id="desktopSearchResults" role="listbox" aria-live="polite">
                            </div>
                        </div>
                    </div>
                </div>

                <div class="nav-icons-section">
                    <button class="icon-button mobile-search-trigger d-lg-none" id="mobileSearchTrigger"
                        aria-label="Open search">
                        <i data-feather="search" aria-hidden="true"></i>
                    </button>

                    <button class="icon-button theme-toggle" id="themeToggle" aria-label="Toggle theme">
                        <i data-feather="sun" class="theme-icon theme-icon-light" aria-hidden="true"></i>
                        <i data-feather="moon" class="theme-icon theme-icon-dark" aria-hidden="true"></i>
                    </button>

                    <button class="icon-button notification-bell" id="notificationBell"
                        aria-label="Admin notifications">
                        <i data-feather="bell" aria-hidden="true"></i>
                        <!-- REMOVED: notification badge span -->
                    </button>

                    <!-- Add this notification dropdown -->
                    <div class="notification-dropdown" id="notificationDropdown" style="display: none;">
                        <div class="notification-dropdown-header">
                            <h4 class="notification-dropdown-title">
                                <i data-feather="bell"></i>
                                Notifications
                            </h4>
                            <div class="notification-dropdown-actions">
                                <button class="notification-action-btn" id="markAllNotificationsReadBtn"
                                    title="Mark all as read">
                                    <i data-feather="check"></i>
                                </button>
                                <button class="notification-dropdown-close" id="closeNotificationDropdown"
                                    title="Close">
                                    <i data-feather="x"></i>
                                </button>
                            </div>
                        </div>
                        <div class="notification-dropdown-content" id="notificationDropdownContent">
                            <div class="notification-loading">Loading notifications...</div>
                        </div>
                        <div class="notification-dropdown-footer">
                            <button class="btn-text" id="viewAllNotificationsBtn">
                                <i data-feather="external-link"></i>
                                View All Notifications
                            </button>
                        </div>
                    </div>

                    <div class="dropdown">
                        <button class="avatar-menu" data-bs-toggle="dropdown" aria-expanded="false" id="avatarMenu">
                            <i data-feather="user" aria-hidden="true"></i>
                        </button>
                        <ul class="dropdown-menu dropdown-menu-end dropdown-menu-cinebrain" id="userDropdown">
                        </ul>
                    </div>
                </div>
            </div>
        </nav>
    </header>

    <!-- Mobile Search Overlay -->
    <div class="mobile-search-overlay" id="mobileSearchOverlay">
        <div class="container">
            <div class="mobile-search-header">
                <button class="btn-back" id="closeMobileSearch">
                    <i data-feather="arrow-left"></i>
                </button>
                <div class="mobile-search-input-wrapper">
                    <i data-feather="search" class="search-icon"></i>
                    <input type="text" class="form-control search-input" id="mobileSearchInput"
                        placeholder="Search admin content..." autocomplete="off" autofocus>
                </div>
            </div>
            <div class="search-results mobile-results" id="mobileSearchResults">
            </div>
        </div>
    </div>

    <main id="main-content" class="admin-main" role="main">
        <!-- Dashboard Content -->
        <div class="container-fluid admin-container">

            <!-- Enhanced Real-time Status Bar -->
            <div class="row mb-3">
                <div class="col-12">
                    <div class="status-bar enhanced-status-bar">
                        <div class="status-main-indicators">
                            <div class="status-item">
                                <span class="status-dot pulse-green" id="mainStatusDot"></span>
                                <span class="status-text" id="mainStatusText">System Online</span>
                            </div>
                            <div class="status-item d-none d-sm-flex">
                                <i data-feather="database" class="status-icon"></i>
                                <span class="status-text">DB: <span id="dbStatus">Connected</span></span>
                            </div>
                            <div class="status-item d-none d-md-flex">
                                <i data-feather="zap" class="status-icon"></i>
                                <span class="status-text">Cache: <span id="cacheStatus">Active</span></span>
                            </div>
                            <div class="status-item d-none d-lg-flex">
                                <i data-feather="wifi" class="status-icon"></i>
                                <span class="status-text">APIs: <span id="apiStatus">Online</span></span>
                            </div>
                        </div>

                        <div class="status-meta-info d-none d-sm-flex">
                            <div class="status-item">
                                <i data-feather="clock" class="status-icon"></i>
                                <span class="status-text">Last: <span id="lastUpdate">--</span></span>
                            </div>
                            <div class="status-item">
                                <i data-feather="activity" class="status-icon"></i>
                                <span class="status-text">Live: <span id="liveConnectionStatus">ON</span></span>
                            </div>
                        </div>

                        <div class="status-actions">
                            <button class="btn btn-sm btn-outline-success health-quick-btn" id="quickHealthCheck"
                                title="Quick Health Check">
                                <i data-feather="heart"></i>
                                <span class="d-none d-sm-inline">Health</span>
                            </button>
                            <button class="btn btn-sm btn-outline-primary refresh-btn-mobile" id="refreshDashboard"
                                title="Refresh Dashboard">
                                <i data-feather="rotate-cw"></i>
                                <span class="d-none d-sm-inline">Refresh</span>
                            </button>
                        </div>
                    </div>
                </div>
            </div>

            <!-- System Health Section -->
            <div class="row mb-4">
                <div class="col-12">
                    <div class="section-header">
                        <h2 class="section-title">
                            <i data-feather="activity"></i>
                            System Monitoring
                        </h2>
                        <div class="section-actions">
                            <select class="form-select form-select-sm" id="monitoringRefreshRate">
                                <option value="10000">10s</option>
                                <option value="30000" selected>30s</option>
                                <option value="60000">1m</option>
                            </select>
                        </div>
                    </div>
                </div>
            </div>

            <!-- System Health Cards -->
            <div class="row g-3 mb-4">
                <!-- Database Health -->
                <div class="col-lg-3 col-md-6">
                    <div class="system-health-card" id="databaseHealthCard">
                        <div class="health-card-header">
                            <div class="health-card-icon database-icon">
                                <i data-feather="database"></i>
                            </div>
                            <div class="health-status-indicator" id="dbHealthStatus">
                                <span class="health-dot"></span>
                            </div>
                        </div>
                        <div class="health-card-content">
                            <h4 class="health-card-title">Database</h4>
                            <div class="health-metrics">
                                <div class="metric">
                                    <span class="metric-label">Status:</span>
                                    <span class="metric-value" id="dbConnectionStatus">--</span>
                                </div>
                                <div class="metric">
                                    <span class="metric-label">Response:</span>
                                    <span class="metric-value" id="dbResponseTime">--ms</span>
                                </div>
                                <div class="metric">
                                    <span class="metric-label">Connections:</span>
                                    <span class="metric-value" id="dbConnections">--</span>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Cache Health -->
                <div class="col-lg-3 col-md-6">
                    <div class="system-health-card" id="cacheHealthCard">
                        <div class="health-card-header">
                            <div class="health-card-icon cache-icon">
                                <i data-feather="zap"></i>
                            </div>
                            <div class="health-status-indicator" id="cacheHealthStatus">
                                <span class="health-dot"></span>
                            </div>
                        </div>
                        <div class="health-card-content">
                            <h4 class="health-card-title">Cache</h4>
                            <div class="health-metrics">
                                <div class="metric">
                                    <span class="metric-label">Type:</span>
                                    <span class="metric-value" id="cacheType">--</span>
                                </div>
                                <div class="metric">
                                    <span class="metric-label">Hit Rate:</span>
                                    <span class="metric-value" id="cacheHitRate">--%</span>
                                </div>
                                <div class="metric">
                                    <span class="metric-label">Memory:</span>
                                    <span class="metric-value" id="cacheMemory">--MB</span>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- API Services -->
                <div class="col-lg-3 col-md-6">
                    <div class="system-health-card" id="servicesHealthCard">
                        <div class="health-card-header">
                            <div class="health-card-icon services-icon">
                                <i data-feather="globe"></i>
                            </div>
                            <div class="health-status-indicator" id="servicesHealthStatus">
                                <span class="health-dot"></span>
                            </div>
                        </div>
                        <div class="health-card-content">
                            <h4 class="health-card-title">External APIs</h4>
                            <div class="health-metrics">
                                <div class="metric">
                                    <span class="metric-label">TMDB:</span>
                                    <span class="metric-value" id="tmdbStatus">--</span>
                                </div>
                                <div class="metric">
                                    <span class="metric-label">YouTube:</span>
                                    <span class="metric-value" id="youtubeStatus">--</span>
                                </div>
                                <div class="metric">
                                    <span class="metric-label">Cloudinary:</span>
                                    <span class="metric-value" id="cloudinaryStatus">--</span>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- System Performance -->
                <div class="col-lg-3 col-md-6">
                    <div class="system-health-card" id="performanceHealthCard">
                        <div class="health-card-header">
                            <div class="health-card-icon performance-icon">
                                <i data-feather="cpu"></i>
                            </div>
                            <div class="health-status-indicator" id="performanceHealthStatus">
                                <span class="health-dot"></span>
                            </div>
                        </div>
                        <div class="health-card-content">
                            <h4 class="health-card-title">Performance</h4>
                            <div class="health-metrics">
                                <div class="metric">
                                    <span class="metric-label">CPU:</span>
                                    <span class="metric-value" id="cpuUsage">--%</span>
                                </div>
                                <div class="metric">
                                    <span class="metric-label">Memory:</span>
                                    <span class="metric-value" id="memoryUsage">--%</span>
                                </div>
                                <div class="metric">
                                    <span class="metric-label">Disk:</span>
                                    <span class="metric-value" id="diskUsage">--%</span>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <!-- Email Preferences Section -->
            <div class="row mb-4">
                <div class="col-12">
                    <div class="dashboard-card">
                        <div class="card-header">
                            <h3 class="card-title">
                                <i data-feather="mail"></i>
                                Email Preferences
                            </h3>
                            <div class="card-actions">
                                <button class="btn btn-outline-primary btn-sm" id="toggleEmailPreferences">
                                    <i data-feather="chevron-down"></i>
                                    <span class="d-none d-sm-inline">Configure Alerts</span>
                                </button>
                            </div>
                        </div>
                        <div class="card-body">
                            <div class="email-preferences-section" id="emailPreferencesSection" style="display: none;">
                                <div id="emailPreferencesContent">
                                    <!-- Email preferences content will be loaded here -->
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <!-- Real-time Alerts Section -->
            <div class="row mb-4" id="alertsSection" style="display: none;">
                <div class="col-12">
                    <div class="dashboard-card alert-dashboard-card">
                        <div class="card-header">
                            <h3 class="card-title">
                                <i data-feather="alert-triangle"></i>
                                Active Alerts
                                <span class="badge badge-danger" id="activeAlertsCount">0</span>
                            </h3>
                            <div class="card-actions">
                                <button class="btn btn-outline-secondary btn-sm" id="dismissAllAlerts">
                                    <i data-feather="x"></i>
                                    <span class="d-none d-sm-inline">Dismiss All</span>
                                </button>
                            </div>
                        </div>
                        <div class="card-body">
                            <div class="alerts-container" id="realTimeAlerts">
                                <!-- Real-time alerts will be populated -->
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Mobile Overview Grid -->
            <div class="row mb-3 d-md-none">
                <div class="col-12">
                    <div class="mobile-overview-scroll">
                        <div class="mobile-overview-cards" id="mobileOverviewCards">
                            <!-- Mobile overview cards will be generated -->
                        </div>
                    </div>
                </div>
            </div>

            <!-- Desktop Overview Cards -->
            <div class="row mb-4 d-none d-md-flex">
                <div class="col-12">
                    <h2 class="section-title">
                        <i data-feather="grid"></i>
                        Overview
                    </h2>
                </div>
            </div>

            <div class="row g-3 mb-4 d-none d-md-flex" id="overviewCards">
                <!-- Desktop cards will be dynamically generated -->
            </div>

            <!-- Quick Actions will be dynamically inserted here by JavaScript -->

            <!-- Mobile Quick Stats -->
            <div class="row mb-3 d-md-none">
                <div class="col-12">
                    <h3 class="mobile-section-title">Quick Stats</h3>
                    <div class="mobile-stats-grid" id="mobileStatsGrid">
                        <!-- Mobile stats will be generated -->
                    </div>
                </div>
            </div>

            <!-- Enhanced Charts Section -->
            <div class="row mb-4">
                <!-- User Growth Chart -->
                <div class="col-12 col-xl-6 mb-3 mb-xl-0">
                    <div class="dashboard-card">
                        <div class="card-header">
                            <h3 class="card-title">
                                <i data-feather="trending-up"></i>
                                <span class="d-none d-sm-inline">User Growth & Activity</span>
                                <span class="d-sm-none">User Activity</span>
                            </h3>
                            <div class="card-actions">
                                <select class="form-select form-select-sm" id="activityPeriod">
                                    <option value="7d">7d</option>
                                    <option value="30d" selected>30d</option>
                                    <option value="90d" class="d-none d-sm-block">90d</option>
                                </select>
                            </div>
                        </div>
                        <div class="card-body">
                            <div class="chart-container">
                                <canvas id="userGrowthChart"></canvas>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- System Performance Chart -->
                <div class="col-12 col-xl-6">
                    <div class="dashboard-card">
                        <div class="card-header">
                            <h3 class="card-title">
                                <i data-feather="activity"></i>
                                <span class="d-none d-sm-inline">System Performance</span>
                                <span class="d-sm-none">Performance</span>
                            </h3>
                            <div class="card-actions">
                                <span class="performance-indicator" id="performanceIndicator">
                                    <span class="performance-dot"></span>
                                    <span class="performance-text">Optimal</span>
                                </span>
                            </div>
                        </div>
                        <div class="card-body">
                            <div class="chart-container">
                                <canvas id="systemPerformanceChart"></canvas>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Second Row Charts -->
            <div class="row mb-4">
                <!-- Content Distribution Chart -->
                <div class="col-12 col-xl-6 mb-3 mb-xl-0">
                    <div class="dashboard-card">
                        <div class="card-header">
                            <h3 class="card-title">
                                <i data-feather="pie-chart"></i>
                                <span class="d-none d-sm-inline">Content Distribution</span>
                                <span class="d-sm-none">Content</span>
                            </h3>
                        </div>
                        <div class="card-body">
                            <div class="chart-container">
                                <canvas id="contentDistributionChart"></canvas>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Service Status Chart -->
                <div class="col-12 col-xl-6">
                    <div class="dashboard-card">
                        <div class="card-header">
                            <h3 class="card-title">
                                <i data-feather="settings"></i>
                                <span class="d-none d-sm-inline">Service Status</span>
                                <span class="d-sm-none">Services</span>
                            </h3>
                        </div>
                        <div class="card-body">
                            <div class="services-grid" id="servicesStatusGrid">
                                <!-- Service status indicators will be populated -->
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Activity & Support Section -->
            <div class="row mb-4">
                <!-- Recent Activity -->
                <div class="col-12 col-lg-6 mb-3 mb-lg-0">
                    <div class="dashboard-card">
                        <div class="card-header">
                            <h3 class="card-title">
                                <i data-feather="clock"></i>
                                Recent Activity
                                <span class="badge badge-info" id="newActivityBadge" style="display: none;">0</span>
                            </h3>
                            <div class="card-actions">
                                <button class="btn btn-outline-secondary btn-sm" id="viewAllActivity">
                                    <i data-feather="external-link"></i>
                                    <span class="d-none d-sm-inline">View All</span>
                                </button>
                            </div>
                        </div>
                        <div class="card-body">
                            <div class="activity-list mobile-optimized" id="recentActivity">
                                <!-- Activity items will be populated -->
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Support Tickets -->
                <div class="col-12 col-lg-6">
                    <div class="dashboard-card">
                        <div class="card-header">
                            <h3 class="card-title">
                                <i data-feather="message-circle"></i>
                                <span class="d-none d-sm-inline">Support</span>
                                <span class="d-sm-none">Support</span>
                                <span class="badge badge-warning" id="urgentTicketsBadge"
                                    style="display: none;">0</span>
                            </h3>
                            <div class="card-actions">
                                <button class="btn btn-outline-primary btn-sm" id="viewSupportDashboard">
                                    <i data-feather="headphones"></i>
                                    <span class="d-none d-sm-inline">Support Center</span>
                                </button>
                            </div>
                        </div>
                        <div class="card-body">
                            <div class="support-stats mobile-optimized" id="supportStats">
                                <!-- Support statistics -->
                            </div>
                            <div class="recent-tickets mobile-optimized" id="recentTickets">
                                <!-- Recent support items will be populated -->
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Admin Activity & Monitoring -->
            <div class="row mb-4">
                <!-- Admin Activity -->
                <div class="col-12 col-lg-6 mb-3 mb-lg-0">
                    <div class="dashboard-card">
                        <div class="card-header">
                            <h3 class="card-title">
                                <i data-feather="users"></i>
                                Admin Activity
                            </h3>
                            <div class="card-actions">
                                <span class="activity-status" id="adminActivityStatus">
                                    <span class="activity-dot pulse-green"></span>
                                    <span class="activity-text">Active</span>
                                </span>
                            </div>
                        </div>
                        <div class="card-body">
                            <div class="admin-activity-metrics" id="adminActivityMetrics">
                                <!-- Admin activity metrics will be populated -->
                            </div>
                        </div>
                    </div>
                </div>

                <!-- System Notifications -->
                <div class="col-12 col-lg-6">
                    <div class="dashboard-card">
                        <div class="card-header">
                            <h3 class="card-title">
                                <i data-feather="bell"></i>
                                System Notifications
                            </h3>
                            <div class="card-actions">
                                <button class="btn btn-outline-secondary btn-sm" id="markAllNotificationsRead">
                                    <i data-feather="check"></i>
                                    <span class="d-none d-sm-inline">Mark All Read</span>
                                </button>
                            </div>
                        </div>
                        <div class="card-body">
                            <div class="notifications-list" id="systemNotificationsList">
                                <!-- System notifications will be populated -->
                            </div>
                        </div>
                    </div>
                </div>
            </div>

        </div>
    </main>

    <!-- Mobile Navigation -->
    <nav class="mobile-bottom-nav d-md-none" id="mobileBottomNav">
        <div class="mobile-nav-container" id="mobileNavContainer">
        </div>
    </nav>

    <!-- Mobile Menu -->
    <aside class="mobile-menu-overlay" id="mobileMenuOverlay">
        <div class="menu-handle"></div>
        <div class="menu-header">
            <h3 class="menu-title">Admin Menu</h3>
            <button class="menu-close-btn" id="mobileMenuClose">
                <i data-feather="x"></i>
            </button>
        </div>
        <section class="menu-section">
            <div class="mobile-menu-grid" id="mobileMenuGrid">
            </div>
        </section>
        <section class="menu-section">
            <h4 class="menu-section-title" id="userSectionTitle">Admin Tools</h4>
            <div class="mobile-menu-list" id="mobileMenuList">
            </div>
        </section>
    </aside>

    <!-- Mobile Backdrop -->
    <div class="mobile-backdrop" id="mobileBackdrop"></div>

    <!-- Scripts -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/feather-icons/dist/feather.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

    <!-- Core JS -->
    <script src="/assets/js/config.js"></script>
    <script src="/assets/js/components/theme-manager.js"></script>
    <script src="/assets/js/components/topbar.js"></script>
    <script src="/assets/js/components/mobile-nav.js"></script>

    <!-- Enhanced Dashboard JS -->
    <script src="/assets/js/admin/main/dashboard.js"></script>
    <script src="/assets/js/admin/main/statistics.js"></script>
    <script src="/assets/js/admin/main/system.js"></script>
    <script src="/assets/js/admin/main/emailpreferences.js"></script>

    <!-- Initialize -->
    <script>
        if (typeof feather !== 'undefined') {
            feather.replace();
        }
    </script>
</body>

</html>



2. dashboard.css 
/**
 * CineBrain Admin Dashboard - Main Dashboard Styles
 * Core layout, typography, and primary dashboard components
 */

:root {
    --admin-primary: #e50914;
    --admin-primary-dark: #991b1b;
    --admin-primary-light: rgba(229, 9, 20, 0.1);
    --admin-primary-rgb: 229, 9, 20;
    --admin-success: #10b981;
    --admin-warning: #f59e0b;
    --admin-danger: #ef4444;
    --admin-info: #3b82f6;

    --card-bg: rgba(255, 255, 255, 0.05);
    --card-border: rgba(255, 255, 255, 0.1);
    --card-hover: rgba(255, 255, 255, 0.08);
    --text-primary: #ffffff;
    --text-secondary: #b3b3b3;
    --text-muted: #888888;

    --dashboard-gap: clamp(8px, 2vw, 24px);
    --card-padding: clamp(12px, 3vw, 24px);
    --border-radius: clamp(8px, 2vw, 16px);
    --header-height: clamp(80px, 12vw, 120px);

    /* Mobile-first responsive variables */
    --mobile-grid-gap: clamp(8px, 2vw, 16px);
    --mobile-card-padding: clamp(12px, 4vw, 20px);
    --mobile-font-base: clamp(14px, 4vw, 16px);
    --mobile-font-small: clamp(12px, 3.5vw, 14px);
    --mobile-font-large: clamp(18px, 5vw, 24px);
    --mobile-icon-size: clamp(16px, 4vw, 20px);

    /* Fluid typography */
    --font-xs: clamp(10px, 2.5vw, 12px);
    --font-sm: clamp(12px, 3vw, 14px);
    --font-md: clamp(14px, 3.5vw, 16px);
    --font-lg: clamp(16px, 4vw, 18px);
    --font-xl: clamp(20px, 5vw, 28px);

    /* Fluid spacing */
    --fluid-0: clamp(4px, 1vw, 8px);
    --fluid-1: clamp(8px, 2vw, 12px);
    --fluid-2: clamp(12px, 3vw, 16px);
    --fluid-3: clamp(16px, 4vw, 24px);
    --fluid-4: clamp(24px, 6vw, 32px);
}

[data-theme="light"],
[data-bs-theme="light"] {
    --card-bg: rgba(255, 255, 255, 0.9);
    --card-border: rgba(0, 0, 0, 0.1);
    --card-hover: rgba(0, 0, 0, 0.05);
    --text-primary: #1a1a1a;
    --text-secondary: #4a4a4a;
    --text-muted: #666666;
}

/* Base Styles */
body {
    font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
    background: var(--bs-body-bg);
    color: var(--text-primary);
    line-height: 1.6;
    font-size: var(--mobile-font-base);
}

.admin-container {
    max-width: 1400px;
    margin: 0 auto;
    padding: var(--dashboard-gap);
    min-height: calc(100vh - var(--navbar-height, 70px));
}

/* Enhanced Mobile-First Header */
.admin-header {
    margin-bottom: var(--dashboard-gap);
    padding: var(--mobile-card-padding);
    background: linear-gradient(135deg, var(--admin-primary), var(--admin-primary-dark));
    border-radius: var(--border-radius);
    color: white;
    position: relative;
    overflow: hidden;
    min-height: var(--header-height);
    display: flex;
    flex-direction: column;
    justify-content: center;
}

.admin-header::before {
    content: '';
    position: absolute;
    top: 0;
    right: 0;
    width: clamp(80px, 20vw, 200px);
    height: clamp(80px, 20vw, 200px);
    background: rgba(255, 255, 255, 0.1);
    border-radius: 50%;
    transform: translate(30%, -30%);
}

.admin-header-content {
    position: relative;
    z-index: 2;
}

.admin-title {
    font-size: var(--mobile-font-large);
    font-weight: 800;
    margin: 0 0 clamp(4px, 1vw, 8px) 0;
    display: flex;
    align-items: center;
    gap: clamp(8px, 2vw, 12px);
    line-height: 1.2;
}

.admin-title svg {
    width: clamp(18px, 5vw, 24px);
    height: clamp(18px, 5vw, 24px);
}

.admin-subtitle {
    font-size: var(--mobile-font-small);
    opacity: 0.9;
    margin: 0;
    font-weight: 400;
    line-height: 1.4;
}

/* Real-time Status Indicator */
.realtime-status {
    position: absolute;
    top: var(--mobile-card-padding);
    right: var(--mobile-card-padding);
    display: flex;
    align-items: center;
    gap: 6px;
    font-size: clamp(10px, 2.5vw, 12px);
    font-weight: 500;
    color: white;
}

.realtime-indicator {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: var(--admin-success);
    animation: pulse-realtime 2s infinite;
}

.realtime-indicator.disconnected {
    background: var(--admin-danger);
    animation: none;
}

@keyframes pulse-realtime {

    0%,
    100% {
        opacity: 1;
        transform: scale(1);
    }

    50% {
        opacity: 0.7;
        transform: scale(1.1);
    }
}

/* Section Titles */
.section-title {
    font-size: var(--font-lg);
    font-weight: 700;
    color: var(--text-primary);
    margin-bottom: var(--fluid-2);
    display: flex;
    align-items: center;
    gap: var(--fluid-1);
}

.section-title i {
    width: 20px;
    height: 20px;
    stroke: var(--admin-primary);
}

.mobile-section-title {
    font-size: var(--font-md);
    font-weight: 600;
    color: var(--text-primary);
    margin-bottom: var(--fluid-2);
}

/* Section Headers */
.section-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: var(--fluid-3);
    flex-wrap: wrap;
    gap: var(--fluid-2);
}

.section-actions {
    display: flex;
    align-items: center;
    gap: var(--fluid-1);
}

/* FIXED: Ensure Quick Actions are visible and properly styled on mobile */
.quick-actions {
    display: grid !important;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--mobile-grid-gap);
    margin-bottom: var(--dashboard-gap);
}

@media (min-width: 576px) {
    .quick-actions {
        grid-template-columns: repeat(3, 1fr);
    }
}

@media (min-width: 768px) {
    .quick-actions {
        grid-template-columns: repeat(6, 1fr);
    }
}

/* FIXED: Ensure Quick Actions row is always visible */
#quickActionsRow {
    display: block !important;
}

.quick-actions .action-btn {
    display: flex !important;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    min-height: clamp(80px, 18vw, 100px);
    background: var(--card-bg);
    border: 1px solid var(--card-border);
    border-radius: var(--border-radius);
    padding: var(--mobile-card-padding);
    text-decoration: none;
    color: var(--text-primary);
    transition: all 0.3s ease;
    gap: clamp(6px, 1.5vw, 8px);
    cursor: pointer;
    position: relative;
    -webkit-tap-highlight-color: transparent;
}

.quick-actions .action-btn:hover,
.quick-actions .action-btn:focus,
.quick-actions .action-btn:active {
    background: var(--card-hover);
    color: var(--text-primary);
    text-decoration: none;
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(229, 9, 20, 0.2);
}

@media (hover: none) and (pointer: coarse) {
    .quick-actions .action-btn:active {
        transform: scale(0.98);
    }
}

.quick-actions .action-btn svg,
.quick-actions .action-btn i {
    width: clamp(18px, 5vw, 24px);
    height: clamp(18px, 5vw, 24px);
    stroke: var(--admin-primary);
    flex-shrink: 0;
}

.quick-actions .action-btn span {
    font-weight: 500;
    font-size: var(--mobile-font-small);
    text-align: center;
    line-height: 1.2;
    color: var(--text-primary);
}

/* FIXED: Mobile-specific Quick Actions positioning */
@media (max-width: 767px) {
    #quickActionsRow {
        order: 1;
        /* Ensure it appears early in the layout */
    }

    .quick-actions {
        padding: 0;
        margin: var(--fluid-3) 0;
    }

    .quick-actions .action-btn {
        min-height: 70px;
        padding: var(--fluid-2);
    }

    .quick-actions .action-btn span {
        font-size: var(--font-xs);
    }
}

.action-btn {
    background: var(--card-bg);
    border: 1px solid var(--card-border);
    border-radius: var(--border-radius);
    padding: var(--mobile-card-padding);
    text-decoration: none;
    color: var(--text-primary);
    transition: all 0.3s ease;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: clamp(6px, 1.5vw, 8px);
    cursor: pointer;
    min-height: clamp(80px, 18vw, 100px);
    justify-content: center;
    position: relative;
    -webkit-tap-highlight-color: transparent;
}

.action-btn:hover,
.action-btn:focus,
.action-btn:active {
    background: var(--card-hover);
    color: var(--text-primary);
    text-decoration: none;
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(229, 9, 20, 0.2);
}

@media (hover: none) and (pointer: coarse) {
    .action-btn:active {
        transform: scale(0.98);
    }
}

.action-btn svg,
.action-btn i {
    width: clamp(18px, 5vw, 24px);
    height: clamp(18px, 5vw, 24px);
    stroke: var(--admin-primary);
    flex-shrink: 0;
}

.action-btn span {
    font-weight: 500;
    font-size: var(--mobile-font-small);
    text-align: center;
    line-height: 1.2;
    color: var(--text-primary);
}

/* Mobile Overview Scroll */
.mobile-overview-scroll {
    overflow-x: auto;
    overflow-y: hidden;
    -webkit-overflow-scrolling: touch;
    scrollbar-width: none;
    -ms-overflow-style: none;
}

.mobile-overview-scroll::-webkit-scrollbar {
    display: none;
}

.mobile-overview-cards {
    display: flex;
    gap: var(--fluid-2);
    padding-bottom: var(--fluid-1);
}

.mobile-overview-card {
    min-width: 140px;
    background: var(--card-bg);
    backdrop-filter: blur(20px);
    border: 1px solid var(--card-border);
    border-left: 3px solid var(--card-color);
    border-radius: var(--border-radius);
    padding: var(--fluid-3);
    flex-shrink: 0;
}

.mobile-card-value {
    font-size: var(--font-xl);
    font-weight: 800;
    color: var(--text-primary);
    margin-bottom: var(--fluid-0);
}

.mobile-card-label {
    font-size: var(--font-xs);
    color: var(--text-secondary);
}

/* Mobile Stats Grid */
.mobile-stats-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--fluid-2);
}

.mobile-stat-item {
    background: var(--card-bg);
    backdrop-filter: blur(20px);
    border: 1px solid var(--card-border);
    border-radius: var(--border-radius);
    padding: var(--fluid-2);
    text-align: center;
}

.mobile-stat-value {
    font-size: var(--font-lg);
    font-weight: 700;
    color: var(--stat-color);
}

.mobile-stat-label {
    font-size: var(--font-xs);
    color: var(--text-secondary);
    margin-top: var(--fluid-0);
}

/* Desktop Overview Cards */
.overview-card {
    background: var(--card-bg);
    backdrop-filter: blur(20px);
    border: 1px solid var(--card-border);
    border-left: 3px solid var(--card-color);
    border-radius: var(--border-radius);
    padding: var(--card-padding);
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    height: 100%;
}

.overview-card:hover {
    background: var(--card-hover);
    transform: translateY(-2px);
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.2);
}

.overview-card-header {
    margin-bottom: var(--fluid-2);
}

.overview-card-title {
    font-size: var(--font-sm);
    font-weight: 600;
    color: var(--text-secondary);
    display: flex;
    align-items: center;
    gap: var(--fluid-1);
}

.overview-card-icon {
    width: 18px;
    height: 18px;
    stroke: var(--card-color);
}

.overview-card-value {
    font-size: var(--font-xl);
    font-weight: 800;
    color: var(--text-primary);
    margin-bottom: var(--fluid-1);
}

.overview-card-change {
    font-size: var(--font-xs);
    font-weight: 600;
    display: flex;
    align-items: center;
    gap: 4px;
}

.change-icon {
    width: 14px;
    height: 14px;
}

.change-positive {
    color: var(--admin-success);
}

.change-negative {
    color: var(--admin-danger);
}

.change-neutral {
    color: var(--text-secondary);
}

/* Dashboard Cards */
.dashboard-card {
    background: var(--card-bg);
    backdrop-filter: blur(20px);
    border: 1px solid var(--card-border);
    border-radius: var(--border-radius);
    overflow: hidden;
    height: 100%;
    display: flex;
    flex-direction: column;
}

.dashboard-card .card-header {
    padding: var(--fluid-3);
    border-bottom: 1px solid var(--card-border);
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--fluid-2);
    flex-wrap: wrap;
}

.dashboard-card .card-title {
    font-size: var(--font-md);
    font-weight: 600;
    color: var(--text-primary);
    margin: 0;
    display: flex;
    align-items: center;
    gap: var(--fluid-1);
}

.dashboard-card .card-title i {
    width: 20px;
    height: 20px;
    stroke: var(--admin-primary);
}

.card-actions {
    display: flex;
    align-items: center;
    gap: var(--fluid-1);
}

.dashboard-card .card-body {
    padding: var(--fluid-3);
    flex: 1;
}

/* ==================== Table Styles for Activities & Tickets ==================== */
.table-responsive-mobile {
    overflow-x: auto;
    -webkit-overflow-scrolling: touch;
    scrollbar-width: none;
    -ms-overflow-style: none;
}

.table-responsive-mobile::-webkit-scrollbar {
    display: none;
}

.data-table {
    width: 100%;
    border-collapse: collapse;
    font-size: var(--font-sm);
    min-width: 500px;
}

.data-table thead {
    border-bottom: 2px solid var(--card-border);
    background: rgba(var(--admin-primary-rgb), 0.03);
}

.data-table th {
    text-align: left;
    padding: var(--fluid-2) var(--fluid-3);
    font-weight: 600;
    color: var(--text-secondary);
    text-transform: uppercase;
    font-size: var(--font-xs);
    letter-spacing: 0.5px;
    white-space: nowrap;
}

.data-table tbody tr {
    border-bottom: 1px solid var(--card-border);
    transition: all 0.2s ease;
}

.data-table tbody tr:hover {
    background: var(--card-hover);
    transform: translateX(2px);
}

.data-table tbody tr:last-child {
    border-bottom: none;
}

.data-table td {
    padding: var(--fluid-3);
    color: var(--text-primary);
    vertical-align: middle;
}

.data-table .activity-info {
    display: flex;
    flex-direction: column;
    gap: var(--fluid-0);
}

.data-table .activity-title {
    font-weight: 600;
    color: var(--text-primary);
    font-size: var(--font-sm);
    line-height: 1.4;
}

.data-table .activity-subtitle {
    color: var(--text-muted);
    font-size: var(--font-xs);
    line-height: 1.3;
}

.data-table .ticket-info {
    display: flex;
    flex-direction: column;
    gap: var(--fluid-0);
}

.data-table .ticket-number {
    font-weight: 600;
    color: var(--admin-primary);
    font-size: var(--font-xs);
    text-decoration: none;
    transition: all 0.2s ease;
}

.data-table .ticket-number:hover {
    text-decoration: underline;
    color: var(--admin-primary-dark);
}

.data-table .ticket-subject {
    font-weight: 500;
    color: var(--text-primary);
    font-size: var(--font-sm);
    line-height: 1.4;
}

.data-table .time-info {
    color: var(--text-muted);
    font-size: var(--font-xs);
    white-space: nowrap;
}

/* Table Badges */
.table-badge {
    display: inline-flex;
    align-items: center;
    gap: 4px;
    padding: 4px 8px;
    border-radius: 6px;
    font-size: var(--font-xs);
    font-weight: 500;
    text-transform: capitalize;
    white-space: nowrap;
}

.table-badge-success {
    background: rgba(16, 185, 129, 0.15);
    color: var(--admin-success);
}

.table-badge-info {
    background: rgba(59, 130, 246, 0.15);
    color: var(--admin-info);
}

.table-badge-warning {
    background: rgba(245, 158, 11, 0.15);
    color: var(--admin-warning);
}

.table-badge-danger {
    background: rgba(239, 68, 68, 0.15);
    color: var(--admin-danger);
}

.table-badge-primary {
    background: rgba(229, 9, 20, 0.15);
    color: var(--admin-primary);
}

/* Support Stats */
.support-stats {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(80px, 1fr));
    gap: var(--fluid-2);
    margin-bottom: var(--fluid-3);
}

.support-stat {
    text-align: center;
    padding: var(--fluid-2);
    background: var(--card-bg);
    border: 1px solid var(--card-border);
    border-radius: var(--border-radius);
}

.support-stat-value {
    font-size: var(--font-lg);
    font-weight: 700;
    color: var(--stat-color);
}

.support-stat-label {
    font-size: var(--font-xs);
    color: var(--text-secondary);
    margin-top: var(--fluid-0);
}

.recent-tickets {
    display: flex;
    flex-direction: column;
    gap: var(--fluid-2);
}

/* Badges */
.badge {
    padding: clamp(2px, 1vw, 4px) clamp(6px, 1.5vw, 8px);
    border-radius: clamp(6px, 1.5vw, 8px);
    font-size: var(--font-xs);
    font-weight: 600;
    white-space: nowrap;
}

.badge-success {
    background: rgba(16, 185, 129, 0.2);
    color: var(--admin-success);
}

.badge-warning {
    background: rgba(245, 158, 11, 0.2);
    color: var(--admin-warning);
}

.badge-danger {
    background: rgba(239, 68, 68, 0.2);
    color: var(--admin-danger);
}

.badge-info {
    background: rgba(59, 130, 246, 0.2);
    color: var(--admin-info);
}

/* Activity List (Legacy compatibility) */
.activity-list {
    display: flex;
    flex-direction: column;
    gap: var(--fluid-2);
}

.activity-item {
    display: flex;
    align-items: flex-start;
    gap: var(--fluid-2);
    padding: var(--fluid-2);
    background: var(--activity-bg, var(--card-bg));
    border: 1px solid var(--card-border);
    border-left: 3px solid var(--activity-color, var(--admin-primary));
    border-radius: var(--border-radius);
    transition: all 0.2s ease;
}

.activity-item:hover {
    background: var(--card-hover);
    transform: translateX(4px);
}

.activity-icon {
    width: 36px;
    height: 36px;
    min-width: 36px;
    background: var(--activity-color, var(--admin-primary));
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
}

.activity-icon i {
    width: 18px;
    height: 18px;
    stroke: currentColor;
}

.activity-content {
    flex: 1;
    min-width: 0;
}

.activity-title {
    font-size: var(--font-sm);
    font-weight: 600;
    color: var(--text-primary);
    margin-bottom: var(--fluid-0);
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
}

.activity-description {
    font-size: var(--font-xs);
    color: var(--text-secondary);
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
}

.activity-time {
    font-size: var(--font-xs);
    color: var(--text-muted);
    white-space: nowrap;
    align-self: center;
}

/* Loading States */
.loading-skeleton {
    background: linear-gradient(90deg,
            var(--card-bg) 25%,
            var(--card-hover) 50%,
            var(--card-bg) 75%);
    background-size: 200% 100%;
    animation: shimmer 2s infinite;
    border-radius: var(--border-radius);
}

.table-loading {
    padding: var(--fluid-3);
}

.table-skeleton-row {
    display: flex;
    align-items: center;
    gap: var(--fluid-2);
    margin-bottom: var(--fluid-2);
}

.table-skeleton-cell {
    background: linear-gradient(90deg,
            var(--card-border) 25%,
            var(--card-hover) 50%,
            var(--card-border) 75%);
    background-size: 200% 100%;
    animation: shimmer 1.5s ease-in-out infinite;
    border-radius: 4px;
}

.table-skeleton-cell.wide {
    flex: 2;
    height: 16px;
}

.table-skeleton-cell.medium {
    flex: 1;
    height: 14px;
}

.table-skeleton-cell.small {
    width: 60px;
    height: 12px;
}

/* Empty State for Tables */
.table-empty-state {
    text-align: center;
    padding: var(--fluid-4);
    color: var(--text-muted);
}

.table-empty-state i {
    width: 32px;
    height: 32px;
    margin-bottom: var(--fluid-2);
    opacity: 0.5;
    stroke: var(--text-muted);
}

/* Activity Animation */
.activity-new {
    animation: slideInHighlight 0.5s ease-out;
}

@keyframes slideInHighlight {
    0% {
        opacity: 0;
        transform: translateY(-10px);
        background: rgba(var(--admin-primary-rgb), 0.2);
    }

    50% {
        background: rgba(var(--admin-primary-rgb), 0.1);
    }

    100% {
        opacity: 1;
        transform: translateY(0);
        background: transparent;
    }
}

/* Animations */
.fade-in {
    animation: fadeIn 0.5s ease-out;
}

.slide-up {
    animation: slideUp 0.5s ease-out;
}

@keyframes fadeIn {
    from {
        opacity: 0;
    }

    to {
        opacity: 1;
    }
}

@keyframes slideUp {
    from {
        opacity: 0;
        transform: translateY(20px);
    }

    to {
        opacity: 1;
        transform: translateY(0);
    }
}

@keyframes shimmer {
    0% {
        background-position: -200% 0;
    }

    100% {
        background-position: 200% 0;
    }
}

/* Responsive Utilities */
@media (max-width: 768px) {
    .data-table {
        font-size: var(--font-xs);
        min-width: 450px;
    }

    .data-table th,
    .data-table td {
        padding: var(--fluid-2);
    }

    .data-table th:nth-child(2),
    .data-table td:nth-child(2) {
        display: none;
    }
}

@media (max-width: 576px) {
    .section-header {
        flex-direction: column;
        align-items: flex-start;
    }

    .admin-container {
        padding: var(--fluid-2);
    }
}

@media (max-width: 480px) {
    .data-table {
        min-width: 350px;
    }

    .table-badge {
        padding: 2px 6px;
        font-size: 10px;
    }

    .data-table .activity-title,
    .data-table .ticket-subject {
        font-size: var(--font-xs);
    }
}

@media (max-width: 360px) {
    .overview-card-value {
        font-size: clamp(18px, 3vw, 24px);
    }
}

/* Tablet Optimizations */
@media (min-width: 768px) and (max-width: 1024px) {
    .overview-card-value {
        font-size: clamp(18px, 3vw, 24px);
    }
}

/* Print Styles */
@media print {

    .admin-header::before,
    .realtime-status,
    .card-actions,
    .mobile-nav,
    .navbar,
    .quick-actions {
        display: none !important;
    }

    .admin-container {
        padding: 0;
    }

    .dashboard-card {
        break-inside: avoid;
    }
}

/* Accessibility */
@media (prefers-reduced-motion: reduce) {

    *,
    *::before,
    *::after {
        animation-duration: 0.01ms !important;
        animation-iteration-count: 1 !important;
        transition-duration: 0.01ms !important;
    }
}

/* High Contrast Mode */
@media (prefers-contrast: high) {

    .dashboard-card,
    .overview-card {
        border-width: 2px;
    }
}

/* Dark scrollbar for webkit browsers */
::-webkit-scrollbar {
    width: 8px;
    height: 8px;
}

::-webkit-scrollbar-track {
    background: var(--card-bg);
}

::-webkit-scrollbar-thumb {
    background: var(--card-border);
    border-radius: 4px;
}

::-webkit-scrollbar-thumb:hover {
    background: var(--text-muted);
}

/* Enhanced Support Items Styling */
.support-item-info {
    display: flex;
    flex-direction: column;
    gap: var(--fluid-0);
}

.support-number {
    font-weight: 600;
    color: var(--admin-primary);
    font-size: var(--font-xs);
    text-decoration: none;
    transition: all 0.2s ease;
    display: flex;
    align-items: center;
}

.support-number:hover {
    text-decoration: underline;
    color: var(--admin-primary-dark);
}

.support-title {
    font-weight: 500;
    color: var(--text-primary);
    font-size: var(--font-sm);
    line-height: 1.4;
}

.company-badge {
    display: inline-flex;
    align-items: center;
    padding: 2px 6px;
    background: rgba(16, 185, 129, 0.15);
    color: var(--admin-success);
    font-size: 10px;
    border-radius: 4px;
    margin-left: 4px;
}

.user-info {
    display: flex;
    flex-direction: column;
    gap: 2px;
}

.user-name {
    font-weight: 500;
    color: var(--text-primary);
    font-size: var(--font-xs);
}

.user-email {
    color: var(--text-muted);
    font-size: 10px;
}

.time-info-detailed {
    display: flex;
    flex-direction: column;
    gap: 2px;
}

.time-ago {
    font-weight: 500;
    color: var(--text-secondary);
    font-size: var(--font-xs);
}

.exact-time {
    color: var(--text-muted);
    font-size: 10px;
    line-height: 1.2;
}

.activity-title {
    font-weight: 600;
    color: var(--admin-primary);
    font-size: var(--font-sm);
    text-decoration: none;
    transition: all 0.2s ease;
    display: flex;
    align-items: center;
    line-height: 1.4;
}

.activity-title:hover {
    text-decoration: underline;
    color: var(--admin-primary-dark);
}

.unread-item {
    background: rgba(59, 130, 246, 0.05);
    border-left: 3px solid var(--admin-info);
}

/* Mobile Responsive Adjustments */
@media (max-width: 768px) {
    .time-info-detailed {
        align-items: flex-end;
        text-align: right;
    }

    .exact-time {
        display: none;
    }

    .support-number,
    .activity-title {
        font-size: 11px;
    }

    .user-name {
        font-size: 11px;
    }
}

/* Enhanced Activity List Styling */
.activity-list-container {
    display: flex;
    flex-direction: column;
    gap: var(--fluid-2);
}

.activity-filter-tabs {
    display: flex;
    gap: var(--fluid-1);
    overflow-x: auto;
    -webkit-overflow-scrolling: touch;
    scrollbar-width: none;
    padding-bottom: var(--fluid-1);
}

.activity-filter-tabs::-webkit-scrollbar {
    display: none;
}

.activity-filter-btn {
    background: none;
    border: 1px solid var(--card-border);
    border-radius: 20px;
    padding: 6px 16px;
    font-size: var(--font-xs);
    font-weight: 500;
    color: var(--text-secondary);
    cursor: pointer;
    transition: all 0.2s ease;
    white-space: nowrap;
}

.activity-filter-btn:hover,
.activity-filter-btn.active {
    background: var(--admin-primary);
    color: white;
    border-color: var(--admin-primary);
}

.activity-table {
    width: 100%;
}

.activity-icon-inline {
    width: 14px;
    height: 14px;
    margin-right: 6px;
    vertical-align: middle;
    color: var(--admin-primary);
}

.activity-details {
    display: block;
    font-size: 10px;
    color: var(--text-muted);
    margin-top: 2px;
    font-style: italic;
}

.activity-user-info {
    display: flex;
    flex-direction: column;
    gap: 2px;
}

.activity-user {
    font-weight: 500;
    color: var(--text-primary);
    font-size: var(--font-xs);
}

.user-role {
    font-size: 10px;
    padding: 2px 6px;
    border-radius: 10px;
    text-transform: uppercase;
    letter-spacing: 0.5px;
}

.user-role.badge-admin {
    background: rgba(229, 9, 20, 0.15);
    color: var(--admin-primary);
}

.activity-type-info {
    display: flex;
    align-items: center;
    gap: 4px;
}

.activity-badge {
    text-transform: capitalize;
}

.priority-indicator {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--text-muted);
}

.priority-indicator.priority-high,
.priority-indicator.priority-urgent,
.priority-indicator.priority-critical {
    background: var(--admin-danger);
    animation: pulse-dot 2s infinite;
}

.activity-load-more {
    display: flex;
    justify-content: center;
    margin-top: var(--fluid-2);
}

/* Activity Row Type Styling */
.activity-type-content {
    border-left-color: var(--admin-success);
}

.activity-type-recommendation {
    border-left-color: var(--admin-warning);
}

.activity-type-support {
    border-left-color: var(--admin-info);
}

.activity-type-admin {
    border-left-color: var(--admin-primary);
}

/* Mobile Responsive */
@media (max-width: 768px) {
    .activity-filter-tabs {
        padding: 0 0 var(--fluid-1) 0;
    }

    .activity-filter-btn {
        padding: 4px 12px;
        font-size: 11px;
    }

    .activity-details {
        display: none;
    }
}

/* Activity Details Styling */
.activity-details {
    display: block;
    font-size: 10px;
    color: var(--text-muted);
    margin-top: 2px;
    font-style: italic;
}

.activity-new {
    position: relative;
}

.activity-new::before {
    content: '';
    position: absolute;
    left: 0;
    top: 0;
    bottom: 0;
    width: 3px;
    background: var(--admin-primary);
    animation: newActivityPulse 2s ease-in-out infinite;
}

@keyframes newActivityPulse {

    0%,
    100% {
        opacity: 1;
    }

    50% {
        opacity: 0.5;
    }
}

.activity-title {
    display: inline-flex;
    align-items: center;
    color: var(--admin-primary);
    text-decoration: none;
    font-weight: 600;
    transition: all 0.2s ease;
}

.activity-title:hover {
    color: var(--admin-primary-dark);
    text-decoration: underline;
}



3. system.css 


:root {
    --text-primary: #ffffff;
    --text-secondary: #b3b3b3;
    --text-muted: #888888;
    --card-bg: rgba(255, 255, 255, 0.05);
    --card-border: rgba(255, 255, 255, 0.1);
    --card-hover: rgba(255, 255, 255, 0.08);
}

[data-theme="light"],
[data-bs-theme="light"] {
    --text-primary: #1a1a1a !important;
    --text-secondary: #4a4a4a !important;
    --text-muted: #666666 !important;
    --card-bg: rgba(255, 255, 255, 0.9) !important;
    --card-border: rgba(0, 0, 0, 0.1) !important;
    --card-hover: rgba(0, 0, 0, 0.05) !important;
}

.section-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: var(--fluid-3);
    flex-wrap: nowrap;
    gap: var(--fluid-2);
}

.status-bar {
    background: var(--card-bg);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    border: 1px solid var(--card-border);
    border-radius: var(--border-radius);
    padding: var(--fluid-2);
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--fluid-2);
    flex-wrap: wrap;
}

.enhanced-status-bar {
    display: grid;
    grid-template-columns: 1fr auto auto;
    gap: var(--fluid-3);
    align-items: center;
}

.status-main-indicators {
    display: flex;
    align-items: center;
    gap: var(--fluid-2);
    flex-wrap: wrap;
}

.status-meta-info {
    display: flex;
    align-items: center;
    gap: var(--fluid-2);
}

.status-item {
    display: flex;
    align-items: center;
    gap: var(--fluid-1);
}

.status-dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    flex-shrink: 0;
    border: 2px solid var(--card-bg);
    box-shadow: 0 0 0 1px rgba(0, 0, 0, 0.1);
}

.pulse-green {
    background: var(--admin-success);
    box-shadow: 0 0 0 0 rgba(16, 185, 129, 0.7);
    animation: pulse-dot 2s infinite;
}

.pulse-yellow {
    background: var(--admin-warning);
    box-shadow: 0 0 0 0 rgba(245, 158, 11, 0.7);
    animation: pulse-dot 2s infinite;
}

.pulse-red {
    background: var(--admin-danger);
    box-shadow: 0 0 0 0 rgba(239, 68, 68, 0.7);
    animation: pulse-dot 2s infinite;
}

@keyframes pulse-dot {
    0% {
        box-shadow: 0 0 0 0 currentColor;
    }

    70% {
        box-shadow: 0 0 0 6px transparent;
    }

    100% {
        box-shadow: 0 0 0 0 transparent;
    }
}

.status-icon {
    width: 16px;
    height: 16px;
    flex-shrink: 0;
    color: var(--text-secondary);
}

.status-text {
    font-size: var(--font-sm);
    color: var(--text-secondary);
}

.status-actions {
    margin-left: auto;
    display: flex;
    align-items: center;
    gap: var(--fluid-1);
}

.status-actions .btn {
    padding: 6px 12px;
    font-size: var(--font-xs);
}

.status-actions .btn i {
    width: 14px;
    height: 14px;
}

.system-health-card {
    background: var(--card-bg);
    backdrop-filter: blur(20px);
    border: 1px solid var(--card-border);
    border-radius: var(--border-radius);
    padding: var(--fluid-3);
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
    overflow: hidden;
}

.system-health-card:hover {
    background: var(--card-hover);
    transform: translateY(-2px);
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.2);
}

.health-card-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: var(--fluid-2);
}

.health-card-icon {
    width: 40px;
    height: 40px;
    border-radius: 12px;
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
}

.health-card-icon.database-icon {
    background: linear-gradient(135deg, #3b82f6, #1d4ed8);
}

.health-card-icon.cache-icon {
    background: linear-gradient(135deg, #f59e0b, #d97706);
}

.health-card-icon.services-icon {
    background: linear-gradient(135deg, #10b981, #059669);
}

.health-card-icon.performance-icon {
    background: linear-gradient(135deg, #8b5cf6, #7c3aed);
}

.health-card-icon i {
    width: 20px;
    height: 20px;
    color: white;
}

.health-status-indicator {
    display: flex;
    align-items: center;
}

.health-dot {
    width: 12px;
    height: 12px;
    border-radius: 50%;
    background: var(--admin-success);
    box-shadow: 0 0 0 0 rgba(16, 185, 129, 0.7);
    animation: pulse-dot 2s infinite;
    border: 2px solid var(--card-bg);
}

.health-dot.warning {
    background: var(--admin-warning);
    box-shadow: 0 0 0 0 rgba(245, 158, 11, 0.7);
}

.health-dot.danger {
    background: var(--admin-danger);
    box-shadow: 0 0 0 0 rgba(239, 68, 68, 0.7);
}

.health-card-title {
    font-size: var(--font-md);
    font-weight: 600;
    color: var(--text-primary) !important;
    margin: 0;
}

.health-metrics {
    display: flex;
    flex-direction: column;
    gap: var(--fluid-1);
}

.metric {
    display: flex;
    align-items: center;
    justify-content: space-between;
    font-size: var(--font-sm);
}

.metric-label {
    color: var(--text-secondary) !important;
}

.metric-value {
    font-weight: 600;
    color: var(--text-primary) !important;
}

.metric-value.healthy {
    color: var(--admin-success);
}

.metric-value.warning {
    color: var(--admin-warning);
}

.metric-value.danger {
    color: var(--admin-danger);
}

.alert-dashboard-card {
    border-left: 3px solid var(--admin-danger);
}

.alerts-container {
    display: flex;
    flex-direction: column;
    gap: var(--fluid-2);
}

.alert-item {
    background: rgba(239, 68, 68, 0.1);
    border: 1px solid rgba(239, 68, 68, 0.2);
    border-left: 3px solid var(--alert-color);
    border-radius: var(--border-radius);
    padding: var(--fluid-3);
    display: flex;
    align-items: flex-start;
    gap: var(--fluid-2);
    transition: all 0.2s ease;
}

.alert-item:hover {
    background: rgba(239, 68, 68, 0.15);
}

.alert-item.warning {
    --alert-color: var(--admin-warning);
    background: rgba(245, 158, 11, 0.1);
    border-color: rgba(245, 158, 11, 0.2);
}

.alert-item.info {
    --alert-color: var(--admin-info);
    background: rgba(59, 130, 246, 0.1);
    border-color: rgba(59, 130, 246, 0.2);
}

.alert-icon {
    width: 20px;
    height: 20px;
    color: var(--alert-color);
    flex-shrink: 0;
    margin-top: 2px;
}

.alert-content {
    flex: 1;
    min-width: 0;
}

.alert-title {
    font-size: var(--font-sm);
    font-weight: 600;
    color: var(--text-primary);
    margin-bottom: var(--fluid-0);
}

.alert-message {
    font-size: var(--font-xs);
    color: var(--text-secondary);
    line-height: 1.4;
}

.alert-time {
    font-size: var(--font-xs);
    color: var(--text-muted);
    white-space: nowrap;
}

.alert-actions {
    display: flex;
    align-items: center;
    gap: var(--fluid-1);
}

.alert-dismiss {
    background: none;
    border: none;
    color: var(--text-muted);
    cursor: pointer;
    padding: 4px;
    border-radius: 4px;
    transition: all 0.2s ease;
}

.alert-dismiss:hover {
    background: var(--card-hover);
    color: var(--text-primary);
}

.performance-indicator {
    display: flex;
    align-items: center;
    gap: var(--fluid-1);
    font-size: var(--font-xs);
    font-weight: 600;
}

.performance-dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: var(--admin-success);
    animation: pulse-dot 2s infinite;
    border: 2px solid var(--card-bg);
    box-shadow: 0 0 0 1px rgba(0, 0, 0, 0.1);
}

.performance-dot.warning {
    background: var(--admin-warning);
}

.performance-dot.danger {
    background: var(--admin-danger);
}

.performance-text {
    color: var(--text-secondary);
}

.services-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(100px, 1fr));
    gap: var(--fluid-2);
}

.service-item {
    text-align: center;
    padding: var(--fluid-2);
    background: var(--card-bg);
    border: 1px solid var(--card-border);
    border-radius: var(--border-radius);
    transition: all 0.2s ease;
}

.service-item:hover {
    background: var(--card-hover);
}

.service-status {
    width: 12px;
    height: 12px;
    border-radius: 50%;
    background: var(--admin-success);
    margin: 0 auto var(--fluid-1);
}

.service-status.warning {
    background: var(--admin-warning);
}

.service-status.danger {
    background: var(--admin-danger);
}

.service-name {
    font-size: var(--font-xs);
    font-weight: 600;
    color: var(--text-primary);
}

.service-info {
    font-size: 10px;
    color: var(--text-secondary);
    margin-top: 2px;
}

.admin-activity-metrics {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
    gap: var(--fluid-2);
}

.activity-metric {
    text-align: center;
    padding: var(--fluid-2);
    background: var(--card-bg);
    border: 1px solid var(--card-border);
    border-radius: var(--border-radius);
}

.activity-metric-value {
    font-size: var(--font-lg);
    font-weight: 700;
    color: var(--text-primary) !important;
    margin-bottom: var(--fluid-0);
}

.activity-metric-label {
    font-size: var(--font-xs);
    color: var(--text-primary) !important;
}

.activity-metric-change {
    font-size: 10px;
    margin-top: 2px;
    color: var(--text-secondary) !important;
}

.activity-status {
    display: flex;
    align-items: center;
    gap: var(--fluid-1);
    font-size: var(--font-xs);
    font-weight: 500;
}

.activity-dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    border: 2px solid var(--card-bg);
    box-shadow: 0 0 0 1px rgba(0, 0, 0, 0.1);
}

.activity-text {
    color: var(--text-secondary);
}

.notifications-list {
    display: flex;
    flex-direction: column;
    gap: var(--fluid-2);
    max-height: 300px;
    overflow-y: auto;
    scrollbar-width: thin;
}

.notification-item {
    display: flex;
    align-items: flex-start;
    gap: var(--fluid-2);
    padding: var(--fluid-2);
    background: var(--card-bg);
    border: 1px solid var(--card-border);
    border-left: 3px solid var(--notification-color, var(--admin-info));
    border-radius: var(--border-radius);
    transition: all 0.2s ease;
}

.notification-item:hover {
    background: var(--card-hover);
}

.notification-item.unread {
    background: rgba(59, 130, 246, 0.05);
    border-left-color: var(--admin-info);
}

.notification-item.urgent {
    background: rgba(239, 68, 68, 0.05);
    border-left-color: var(--admin-danger);
    --notification-color: var(--admin-danger);
}

.notification-icon {
    width: 20px;
    height: 20px;
    color: var(--notification-color, var(--admin-info));
    flex-shrink: 0;
    margin-top: 2px;
}

.notification-content {
    flex: 1;
    min-width: 0;
}

.notification-title {
    font-size: var(--font-sm);
    font-weight: 600;
    color: var(--text-primary);
    margin-bottom: var(--fluid-0);
}

.notification-message {
    font-size: var(--font-xs);
    color: var(--text-secondary);
    line-height: 1.4;
}

.notification-time {
    font-size: var(--font-xs);
    color: var(--text-muted);
    white-space: nowrap;
    margin-top: var(--fluid-0);
}

.notification-dropdown {
    position: fixed;
    top: calc(var(--topbar-height, 70px) + 12px);
    right: 20px;
    width: 400px;
    max-width: calc(100vw - 40px);
    max-height: calc(100vh - 120px);
    background: var(--card-bg);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    border: 1px solid var(--card-border);
    border-radius: var(--border-radius);
    box-shadow: 0 12px 48px rgba(0, 0, 0, 0.4);
    z-index: 1050;
    transform: translateY(-10px) scale(0.95);
    opacity: 0;
    visibility: hidden;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    display: flex;
    flex-direction: column;
    overflow: hidden;
}

.notification-dropdown.show {
    transform: translateY(0) scale(1);
    opacity: 1;
    visibility: visible;
}

.notification-dropdown-header {
    padding: var(--fluid-3);
    border-bottom: 1px solid var(--card-border);
    display: flex;
    align-items: center;
    justify-content: space-between;
    background: var(--card-bg);
    flex-shrink: 0;
}

.notification-dropdown-title {
    font-size: var(--font-md);
    font-weight: 600;
    margin: 0;
    display: flex;
    align-items: center;
    gap: var(--fluid-1);
    color: var(--text-primary);
}

.notification-dropdown-title i {
    width: 18px;
    height: 18px;
    color: var(--admin-primary);
}

.notification-dropdown-actions {
    display: flex;
    align-items: center;
    gap: var(--fluid-1);
}

.notification-action-btn {
    background: none;
    border: 1px solid var(--card-border);
    border-radius: 6px;
    color: var(--text-secondary);
    cursor: pointer;
    padding: 6px;
    transition: all 0.2s ease;
    display: flex;
    align-items: center;
    justify-content: center;
}

.notification-action-btn:hover {
    background: var(--card-hover);
    color: var(--text-primary);
    border-color: var(--admin-primary);
}

.notification-action-btn i {
    width: 14px;
    height: 14px;
}

.notification-dropdown-close {
    background: none;
    border: 1px solid var(--card-border);
    border-radius: 6px;
    color: var(--text-secondary);
    cursor: pointer;
    padding: 6px;
    transition: all 0.2s ease;
    display: flex;
    align-items: center;
    justify-content: center;
}

.notification-dropdown-close:hover {
    background: var(--admin-danger);
    color: white;
    border-color: var(--admin-danger);
}

.notification-dropdown-close i {
    width: 14px;
    height: 14px;
}

.notification-dropdown-content {
    flex: 1;
    overflow-y: auto;
    scrollbar-width: thin;
    scrollbar-color: var(--card-border) transparent;
    min-height: 200px;
    max-height: 60vh;
}

.notification-dropdown-content::-webkit-scrollbar {
    width: 6px;
}

.notification-dropdown-content::-webkit-scrollbar-track {
    background: transparent;
}

.notification-dropdown-content::-webkit-scrollbar-thumb {
    background: var(--card-border);
    border-radius: 3px;
}

.notification-dropdown-content::-webkit-scrollbar-thumb:hover {
    background: var(--text-muted);
}

.notification-dropdown-item {
    display: flex;
    align-items: flex-start;
    gap: var(--fluid-2);
    padding: var(--fluid-3);
    border-bottom: 1px solid var(--card-border);
    cursor: pointer;
    transition: all 0.2s ease;
    position: relative;
    background: var(--card-bg);
}

.notification-dropdown-item:last-child {
    border-bottom: none;
}

.notification-dropdown-item:hover {
    background: var(--card-hover);
}

.notification-dropdown-item.unread {
    background: rgba(59, 130, 246, 0.05);
    border-left: 3px solid var(--admin-info);
}

.notification-dropdown-item.unread::before {
    content: '';
    position: absolute;
    left: var(--fluid-2);
    top: var(--fluid-3);
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: var(--admin-info);
}

.notification-dropdown-item.critical {
    background: rgba(239, 68, 68, 0.05);
    border-left: 3px solid var(--admin-danger);
}

.notification-dropdown-item.critical::before {
    background: var(--admin-danger);
}

.notification-dropdown-icon {
    width: 36px;
    height: 36px;
    min-width: 36px;
    border-radius: 50%;
    background: var(--card-hover);
    display: flex;
    align-items: center;
    justify-content: center;
    margin-top: 2px;
}

.notification-dropdown-item.critical .notification-dropdown-icon {
    background: rgba(239, 68, 68, 0.15);
    color: var(--admin-danger);
}

.notification-dropdown-item.warning .notification-dropdown-icon {
    background: rgba(245, 158, 11, 0.15);
    color: var(--admin-warning);
}

.notification-dropdown-item.success .notification-dropdown-icon {
    background: rgba(16, 185, 129, 0.15);
    color: var(--admin-success);
}

.notification-dropdown-icon i {
    width: 18px;
    height: 18px;
}

.notification-dropdown-body {
    flex: 1;
    min-width: 0;
}

.notification-dropdown-item-title {
    font-size: var(--font-sm);
    font-weight: 600;
    color: var(--text-primary);
    margin-bottom: 4px;
    line-height: 1.4;
}

.notification-dropdown-item-message {
    font-size: var(--font-xs);
    color: var(--text-secondary);
    line-height: 1.4;
    margin-bottom: 6px;
}

.notification-dropdown-item-time {
    font-size: 10px;
    color: var(--text-muted);
    font-weight: 500;
}

.notification-dropdown-footer {
    padding: var(--fluid-2) var(--fluid-3);
    border-top: 1px solid var(--card-border);
    text-align: center;
    background: var(--card-bg);
    flex-shrink: 0;
}

.btn-text {
    background: none;
    border: none;
    color: var(--admin-primary);
    font-size: var(--font-sm);
    font-weight: 500;
    cursor: pointer;
    padding: var(--fluid-1) var(--fluid-2);
    border-radius: 6px;
    transition: all 0.2s ease;
    display: inline-flex;
    align-items: center;
    gap: var(--fluid-1);
    text-decoration: none;
}

.btn-text:hover {
    background: var(--card-hover);
    color: var(--admin-primary-dark);
}

.btn-text i {
    width: 14px;
    height: 14px;
}

.notification-empty {
    text-align: center;
    padding: var(--fluid-4);
    color: var(--text-muted);
}

.notification-empty i {
    width: 48px;
    height: 48px;
    margin-bottom: var(--fluid-2);
    opacity: 0.5;
    color: var(--text-muted);
}

.notification-empty p {
    margin: 0;
    font-size: var(--font-sm);
}

.notification-error {
    text-align: center;
    padding: var(--fluid-4);
    color: var(--text-muted);
}

.notification-error i {
    width: 48px;
    height: 48px;
    margin-bottom: var(--fluid-2);
    opacity: 0.5;
    color: var(--admin-danger);
}

.notification-error p {
    margin: 0 0 var(--fluid-2) 0;
    font-size: var(--font-sm);
}

.notification-error .btn-text {
    background: var(--card-hover);
    border: 1px solid var(--card-border);
    padding: var(--fluid-1) var(--fluid-2);
}

.notification-error .btn-text:hover {
    background: var(--admin-primary);
    color: white;
    border-color: var(--admin-primary);
}

.notification-loading {
    text-align: center;
    padding: var(--fluid-4);
    color: var(--text-secondary);
    font-size: var(--font-sm);
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: var(--fluid-2);
}

.notification-loading::before {
    content: '';
    width: 24px;
    height: 24px;
    border: 3px solid var(--card-border);
    border-top: 3px solid var(--admin-primary);
    border-radius: 50%;
    animation: spin 1s linear infinite;
}

@keyframes spin {
    0% {
        transform: rotate(0deg);
    }

    100% {
        transform: rotate(360deg);
    }
}

.health-card-loading {
    position: relative;
    overflow: hidden;
}

.health-card-loading::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.1), transparent);
    animation: loading-shimmer 2s infinite;
}

@keyframes loading-shimmer {
    0% {
        left: -100%;
    }

    100% {
        left: 100%;
    }
}

.notification-pulse {
    animation: notificationPulse 1s ease-in-out;
}

@keyframes notificationPulse {
    0% {
        transform: scale(1);
    }

    50% {
        transform: scale(1.2);
    }

    100% {
        transform: scale(1);
    }
}

@media (max-width: 768px) {
    .enhanced-status-bar {
        grid-template-columns: 1fr auto;
        gap: var(--fluid-1);
        align-items: center;
    }

    .status-meta-info {
        display: none;
    }

    .status-main-indicators {
        gap: var(--fluid-1);
        flex-wrap: nowrap;
        overflow: hidden;
    }

    .status-item:nth-child(n+3) {
        display: none;
    }

    .health-quick-btn,
    .refresh-btn-mobile {
        padding: 3px 6px !important;
        font-size: 10px !important;
        min-height: unset !important;
        line-height: 1.2 !important;
        border-radius: 4px !important;
    }

    .health-quick-btn i,
    .refresh-btn-mobile i {
        width: 10px !important;
        height: 10px !important;
        margin-right: 2px !important;
    }

    .health-quick-btn span,
    .refresh-btn-mobile span {
        display: none !important;
    }

    .status-actions {
        gap: 4px;
        margin-left: var(--fluid-1);
    }

    .row.g-3.mb-4 {
        display: grid !important;
        grid-template-columns: 1fr 1fr;
        gap: var(--fluid-2);
        margin-bottom: var(--fluid-4) !important;
    }

    .row.g-3.mb-4>.col-lg-3.col-md-6 {
        grid-column: span 1;
        max-width: none;
        flex: none;
        width: 100%;
    }

    .system-health-card {
        padding: var(--fluid-2);
    }

    .health-card-icon {
        width: 32px;
        height: 32px;
    }

    .health-card-icon i {
        width: 16px;
        height: 16px;
    }

    .health-card-title {
        font-size: var(--font-sm);
    }

    .metric {
        font-size: var(--font-xs);
    }

    .admin-activity-metrics {
        display: grid !important;
        grid-template-columns: 1fr 1fr !important;
        gap: var(--fluid-2) !important;
    }

    .activity-metric {
        padding: var(--fluid-2);
        min-height: 70px;
        display: flex;
        flex-direction: column;
        justify-content: center;
    }

    .activity-metric-value {
        font-size: var(--font-md);
        margin-bottom: 2px;
    }

    .activity-metric-label {
        font-size: 10px;
        line-height: 1.2;
    }

    .activity-metric-change {
        font-size: 9px;
        margin-top: 1px;
    }

    .services-grid {
        grid-template-columns: repeat(3, 1fr);
    }

    .notification-dropdown {
        top: calc(var(--topbar-height, 70px) + 8px);
        right: 10px;
        left: 10px;
        width: auto;
        max-width: none;
        max-height: calc(100vh - 100px);
    }

    .notification-dropdown-header {
        padding: var(--fluid-2);
    }

    .notification-dropdown-title {
        font-size: var(--font-sm);
    }

    .notification-dropdown-item {
        padding: var(--fluid-2);
        gap: var(--fluid-1);
    }

    .notification-dropdown-icon {
        width: 32px;
        height: 32px;
        min-width: 32px;
    }

    .notification-dropdown-icon i {
        width: 16px;
        height: 16px;
    }

    .notification-dropdown-item-title {
        font-size: var(--font-xs);
    }

    .notification-dropdown-item-message {
        font-size: 11px;
    }

    .notification-dropdown-item-time {
        font-size: 9px;
    }

    .notification-dropdown-footer {
        padding: var(--fluid-1) var(--fluid-2);
    }

    .btn-text {
        font-size: var(--font-xs);
        padding: var(--fluid-1);
    }
}

@media (max-width: 576px) {
    .section-header {
        flex-wrap: nowrap;
        gap: var(--fluid-1);
    }

    .section-title {
        font-size: var(--font-md);
        flex: 1;
        min-width: 0;
        color: var(--text-primary) !important;
    }

    .section-actions {
        flex-shrink: 0;
    }

    .section-actions .form-select {
        font-size: var(--font-xs);
        padding: 2px 6px;
        min-width: 50px;
    }

    .alert-item {
        padding: var(--fluid-2);
        gap: var(--fluid-1);
    }

    .services-grid {
        grid-template-columns: repeat(2, 1fr);
    }
}

@media (max-width: 480px) {
    .enhanced-status-bar {
        padding: var(--fluid-1);
        grid-template-columns: 1fr auto;
        gap: 6px;
    }

    .status-main-indicators {
        gap: 6px;
    }

    .status-item:nth-child(n+2) {
        display: none;
    }

    .health-quick-btn,
    .refresh-btn-mobile {
        padding: 2px 4px !important;
        font-size: 9px !important;
    }

    .health-quick-btn i,
    .refresh-btn-mobile i {
        width: 9px !important;
        height: 9px !important;
        margin-right: 1px !important;
    }

    .status-actions {
        gap: 2px;
    }

    .notification-dropdown {
        top: calc(var(--topbar-height, 70px) + 5px);
        right: 5px;
        left: 5px;
        border-radius: 12px;
    }

    .notification-dropdown-item {
        padding: 12px;
    }

    .notification-action-btn,
    .notification-dropdown-close {
        padding: 4px;
    }

    .notification-action-btn i,
    .notification-dropdown-close i {
        width: 12px;
        height: 12px;
    }
}

@media (max-width: 360px) {
    .health-card-icon {
        width: 32px;
        height: 32px;
    }

    .health-card-icon i {
        width: 16px;
        height: 16px;
    }

    .health-card-title {
        font-size: var(--font-sm);
    }

    .metric {
        font-size: 11px;
    }
}

@media (min-width: 1024px) {
    .enhanced-status-bar {
        grid-template-columns: 2fr 1fr auto;
    }

    .system-health-card:hover {
        transform: translateY(-3px);
        box-shadow: 0 12px 32px rgba(0, 0, 0, 0.3);
    }
}

@media (prefers-contrast: high) {
    .notification-dropdown {
        border-width: 2px;
        box-shadow: 0 8px 32px rgba(0, 0, 0, 0.6);
    }

    .notification-dropdown-item {
        border-bottom-width: 2px;
    }

    .notification-action-btn,
    .notification-dropdown-close {
        border-width: 2px;
    }
}

@media (prefers-reduced-motion: reduce) {

    .health-dot,
    .performance-dot,
    .activity-dot {
        animation: none !important;
    }

    .notification-dropdown {
        transition: opacity 0.2s ease;
    }

    .notification-loading::before {
        animation: none;
    }
}

[data-theme="light"] .health-dot,
[data-theme="light"] .activity-dot,
[data-theme="light"] .status-dot {
    box-shadow: 0 0 0 1px rgba(255, 255, 255, 0.8);
}

[data-theme="dark"] .notification-dropdown {
    background: rgba(20, 20, 20, 0.95);
    box-shadow: 0 12px 48px rgba(0, 0, 0, 0.8);
}

[data-theme="dark"] .notification-dropdown-header {
    background: rgba(20, 20, 20, 0.95);
}

[data-theme="dark"] .notification-dropdown-item {
    background: rgba(20, 20, 20, 0.95);
}

[data-theme="dark"] .notification-dropdown-footer {
    background: rgba(20, 20, 20, 0.95);
}

[data-theme="light"] .notification-dropdown {
    background: rgba(255, 255, 255, 0.95);
    box-shadow: 0 12px 48px rgba(0, 0, 0, 0.15);
}

[data-theme="light"] .notification-dropdown-header {
    background: rgba(255, 255, 255, 0.95);
}

[data-theme="light"] .notification-dropdown-item {
    background: rgba(255, 255, 255, 0.95);
}

[data-theme="light"] .notification-dropdown-footer {
    background: rgba(255, 255, 255, 0.95);
}

.health-card-title,
.activity-metric-value,
.activity-metric-label,
.section-title,
.metric-label,
.metric-value {
    color: var(--text-primary) !important;
}

.activity-metric-change,
.metric-label {
    color: var(--text-secondary) !important;
}

4. statistics.css
/**
 * CineBrain Admin Dashboard - Statistics & Charts Styles
 * Chart containers, performance visualizations, and analytics components
 */

/* Chart Container */
.chart-container {
    position: relative;
    height: clamp(200px, 40vw, 350px);
    width: 100%;
}

@media (max-width: 575px) {
    .chart-container {
        height: clamp(180px, 50vw, 250px);
    }
}

/* Chart Loading States */
.chart-loading {
    display: flex;
    align-items: center;
    justify-content: center;
    height: 100%;
    color: var(--text-secondary);
    font-size: var(--font-sm);
}

.chart-skeleton {
    background: linear-gradient(90deg,
            var(--card-bg) 25%,
            var(--card-hover) 50%,
            var(--card-bg) 75%);
    background-size: 200% 100%;
    animation: shimmer 2s infinite;
    border-radius: var(--border-radius);
    height: 100%;
    width: 100%;
}

/* Chart Error State */
.chart-error {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    height: 100%;
    color: var(--text-muted);
    text-align: center;
    padding: var(--fluid-3);
}

.chart-error i {
    width: 32px;
    height: 32px;
    margin-bottom: var(--fluid-2);
    opacity: 0.5;
}

.chart-error-message {
    font-size: var(--font-sm);
    margin-bottom: var(--fluid-1);
}

.chart-error-details {
    font-size: var(--font-xs);
    color: var(--text-muted);
}

/* Chart Overlay for Interactions */
.chart-overlay {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    pointer-events: none;
    z-index: 10;
}

.chart-tooltip {
    position: absolute;
    background: var(--card-bg);
    backdrop-filter: blur(10px);
    border: 1px solid var(--card-border);
    border-radius: 8px;
    padding: 8px 12px;
    font-size: var(--font-xs);
    color: var(--text-primary);
    pointer-events: none;
    transform: translate(-50%, -100%);
    margin-top: -8px;
    opacity: 0;
    transition: opacity 0.2s ease;
    z-index: 1000;
}

.chart-tooltip.show {
    opacity: 1;
}

.chart-tooltip::after {
    content: '';
    position: absolute;
    top: 100%;
    left: 50%;
    transform: translateX(-50%);
    border: 4px solid transparent;
    border-top-color: var(--card-border);
}

/* System Performance Chart Specific */
.system-performance-chart {
    position: relative;
}

.performance-legend {
    display: flex;
    justify-content: center;
    gap: var(--fluid-3);
    margin-top: var(--fluid-2);
    flex-wrap: wrap;
}

.performance-legend-item {
    display: flex;
    align-items: center;
    gap: var(--fluid-1);
    font-size: var(--font-xs);
    color: var(--text-secondary);
}

.performance-legend-color {
    width: 12px;
    height: 12px;
    border-radius: 2px;
    flex-shrink: 0;
}

.performance-legend-color.cpu {
    background: #ef4444;
}

.performance-legend-color.memory {
    background: #f59e0b;
}

.performance-legend-color.disk {
    background: #8b5cf6;
}

/* Performance Threshold Lines */
.performance-threshold {
    position: absolute;
    left: 0;
    right: 0;
    border-top: 1px dashed var(--admin-warning);
    opacity: 0.5;
    pointer-events: none;
}

.performance-threshold.critical {
    border-color: var(--admin-danger);
    top: 10%;
    /* 90% usage */
}

.performance-threshold.warning {
    border-color: var(--admin-warning);
    top: 30%;
    /* 70% usage */
}

.performance-threshold-label {
    position: absolute;
    right: 4px;
    top: -10px;
    font-size: 10px;
    color: inherit;
    background: var(--card-bg);
    padding: 0 4px;
}

/* User Growth Chart Enhancements */
.user-growth-chart {
    position: relative;
}

.growth-metrics {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(80px, 1fr));
    gap: var(--fluid-2);
    margin-top: var(--fluid-2);
    padding-top: var(--fluid-2);
    border-top: 1px solid var(--card-border);
}

.growth-metric {
    text-align: center;
}

.growth-metric-value {
    font-size: var(--font-md);
    font-weight: 700;
    color: var(--text-primary);
    display: block;
}

.growth-metric-label {
    font-size: var(--font-xs);
    color: var(--text-secondary);
    margin-top: var(--fluid-0);
}

.growth-metric-change {
    font-size: 10px;
    margin-top: 2px;
}

.growth-metric-change.positive {
    color: var(--admin-success);
}

.growth-metric-change.negative {
    color: var(--admin-danger);
}

.growth-metric-change.neutral {
    color: var(--text-muted);
}

/* Content Distribution Chart */
.content-distribution-chart {
    position: relative;
}

.distribution-center-stats {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    text-align: center;
    pointer-events: none;
}

.distribution-total {
    font-size: var(--font-lg);
    font-weight: 800;
    color: var(--text-primary);
    line-height: 1;
}

.distribution-label {
    font-size: var(--font-xs);
    color: var(--text-secondary);
    margin-top: 2px;
}

/* Chart Responsive Adjustments */
@media (max-width: 768px) {
    .chart-container {
        height: clamp(160px, 45vw, 250px);
    }

    .performance-legend {
        gap: var(--fluid-2);
        margin-top: var(--fluid-1);
    }

    .performance-legend-item {
        font-size: 10px;
        gap: 4px;
    }

    .performance-legend-color {
        width: 8px;
        height: 8px;
    }

    .growth-metrics {
        grid-template-columns: repeat(2, 1fr);
        gap: var(--fluid-1);
    }

    .growth-metric-value {
        font-size: var(--font-sm);
    }

    .distribution-total {
        font-size: var(--font-md);
    }

    .chart-tooltip {
        font-size: 11px;
        padding: 6px 8px;
    }
}

@media (max-width: 480px) {
    .chart-container {
        height: clamp(140px, 50vw, 200px);
    }

    .performance-legend {
        flex-direction: column;
        align-items: center;
        gap: var(--fluid-1);
    }

    .growth-metrics {
        grid-template-columns: 1fr 1fr;
    }
}

/* Chart Animation States */
.chart-fade-in {
    animation: chartFadeIn 0.5s ease-out;
}

@keyframes chartFadeIn {
    from {
        opacity: 0;
        transform: translateY(20px);
    }

    to {
        opacity: 1;
        transform: translateY(0);
    }
}

.chart-slide-in-left {
    animation: chartSlideInLeft 0.6s ease-out;
}

@keyframes chartSlideInLeft {
    from {
        opacity: 0;
        transform: translateX(-30px);
    }

    to {
        opacity: 1;
        transform: translateX(0);
    }
}

.chart-slide-in-right {
    animation: chartSlideInRight 0.6s ease-out;
}

@keyframes chartSlideInRight {
    from {
        opacity: 0;
        transform: translateX(30px);
    }

    to {
        opacity: 1;
        transform: translateX(0);
    }
}

/* Chart Data Update Animations */
.chart-updating {
    position: relative;
}

.chart-updating::after {
    content: '';
    position: absolute;
    top: 50%;
    right: 10px;
    width: 16px;
    height: 16px;
    border: 2px solid transparent;
    border-top: 2px solid var(--admin-primary);
    border-radius: 50%;
    animation: chartSpin 1s linear infinite;
}

@keyframes chartSpin {
    0% {
        transform: rotate(0deg);
    }

    100% {
        transform: rotate(360deg);
    }
}

/* Chart Export and Actions */
.chart-actions {
    position: absolute;
    top: 10px;
    right: 10px;
    display: flex;
    gap: 4px;
    opacity: 0;
    transition: opacity 0.2s ease;
}

.chart-container:hover .chart-actions {
    opacity: 1;
}

.chart-action-btn {
    background: var(--card-bg);
    backdrop-filter: blur(10px);
    border: 1px solid var(--card-border);
    border-radius: 4px;
    padding: 4px;
    cursor: pointer;
    transition: all 0.2s ease;
    color: var(--text-secondary);
}

.chart-action-btn:hover {
    background: var(--card-hover);
    color: var(--text-primary);
}

.chart-action-btn i {
    width: 12px;
    height: 12px;
}

/* Chart Zoom Controls */
.chart-zoom-controls {
    display: flex;
    align-items: center;
    gap: var(--fluid-1);
    margin-top: var(--fluid-2);
    justify-content: center;
}

.chart-zoom-btn {
    background: none;
    border: 1px solid var(--card-border);
    border-radius: 4px;
    padding: 4px 8px;
    font-size: var(--font-xs);
    color: var(--text-secondary);
    cursor: pointer;
    transition: all 0.2s ease;
}

.chart-zoom-btn:hover,
.chart-zoom-btn.active {
    background: var(--card-hover);
    color: var(--text-primary);
    border-color: var(--admin-primary);
}

.chart-zoom-btn.active {
    color: var(--admin-primary);
}

/* Real-time Chart Indicators */
.chart-live-indicator {
    position: absolute;
    top: 10px;
    left: 10px;
    display: flex;
    align-items: center;
    gap: 4px;
    font-size: 10px;
    color: var(--admin-success);
    background: rgba(16, 185, 129, 0.1);
    padding: 2px 6px;
    border-radius: 10px;
}

.chart-live-dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--admin-success);
    animation: pulse-dot 2s infinite;
}

/* Chart Period Selector Integration */
.chart-period-controls {
    display: flex;
    align-items: center;
    gap: var(--fluid-1);
    margin-bottom: var(--fluid-2);
}

.chart-period-btn {
    background: none;
    border: 1px solid var(--card-border);
    border-radius: 6px;
    padding: 6px 12px;
    font-size: var(--font-xs);
    color: var(--text-secondary);
    cursor: pointer;
    transition: all 0.2s ease;
}

.chart-period-btn:hover,
.chart-period-btn.active {
    background: var(--admin-primary);
    color: white;
    border-color: var(--admin-primary);
}

/* Chart Statistics Summary */
.chart-stats-summary {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(60px, 1fr));
    gap: var(--fluid-1);
    margin-top: var(--fluid-2);
    padding: var(--fluid-2);
    background: var(--card-bg);
    border-radius: 8px;
    border: 1px solid var(--card-border);
}

.chart-stat-item {
    text-align: center;
}

.chart-stat-value {
    font-size: var(--font-sm);
    font-weight: 600;
    color: var(--text-primary);
    display: block;
}

.chart-stat-label {
    font-size: 10px;
    color: var(--text-secondary);
    text-transform: uppercase;
    letter-spacing: 0.5px;
}

/* Dark/Light Theme Chart Adjustments */
[data-theme="light"] .chart-tooltip {
    background: rgba(255, 255, 255, 0.95);
    border-color: rgba(0, 0, 0, 0.1);
}

[data-theme="light"] .chart-tooltip::after {
    border-top-color: rgba(0, 0, 0, 0.1);
}

[data-theme="light"] .chart-action-btn {
    background: rgba(255, 255, 255, 0.9);
    border-color: rgba(0, 0, 0, 0.1);
}

[data-theme="light"] .performance-threshold {
    opacity: 0.7;
}

/* Print Styles for Charts */
@media print {

    .chart-actions,
    .chart-zoom-controls,
    .chart-live-indicator {
        display: none !important;
    }

    .chart-container {
        height: 300px !important;
        break-inside: avoid;
    }

    .chart-stats-summary {
        break-inside: avoid;
    }
}

/* High Performance Mode */
@media (prefers-reduced-motion: reduce) {

    .chart-fade-in,
    .chart-slide-in-left,
    .chart-slide-in-right,
    .chart-updating::after,
    .chart-live-dot {
        animation: none !important;
    }
}

/* Chart Accessibility Enhancements */
@media (prefers-contrast: high) {
    .chart-tooltip {
        border-width: 2px;
    }

    .performance-threshold {
        border-width: 2px;
        opacity: 1;
    }

    .chart-action-btn {
        border-width: 2px;
    }
}

/* FIXED: Force chart text colors in dark theme */
[data-theme="dark"] .chart-container canvas,
[data-bs-theme="dark"] .chart-container canvas {
    color-scheme: dark;
}

[data-theme="light"] .chart-container canvas,
[data-bs-theme="light"] .chart-container canvas {
    color-scheme: light;
}

/* Force legend text visibility */
.chart-container .chartjs-legend-item {
    color: var(--text-primary) !important;
}

[data-theme="dark"] .chart-container .chartjs-legend-item,
[data-bs-theme="dark"] .chart-container .chartjs-legend-item {
    color: #ffffff !important;
}

[data-theme="light"] .chart-container .chartjs-legend-item,
[data-bs-theme="light"] .chart-container .chartjs-legend-item {
    color: #1a1a1a !important;
}

/* Force center text visibility in doughnut chart */
[data-theme="dark"] .chart-container canvas {
    filter: brightness(1) contrast(1);
}