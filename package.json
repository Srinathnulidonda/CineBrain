{
  "name": "movie-recommendation-frontend",
  "version": "1.0.0",
  "description": "Modern movie and TV show recommendation system frontend",
  "main": "index.html",
  "scripts": {
    "start": "live-server --port=5500 --host=localhost",
    "build": "webpack --mode=production",
    "dev": "webpack --mode=development --watch",
    "test": "jest",
    "lint": "eslint js/**/*.js",
    "format": "prettier --write js/**/*.js css/**/*.css",
    "deploy": "npm run build && node build/deploy.js"
  },
  "dependencies": {
    "bootstrap": "^5.3.0",
    "tailwindcss": "^3.3.0"
  },
  "devDependencies": {
    "webpack": "^5.88.0",
    "webpack-cli": "^5.1.0",
    "live-server": "^1.2.2",
    "jest": "^29.5.0",
    "eslint": "^8.44.0",
    "prettier": "^2.8.8"
  },
  "keywords": ["movie", "recommendation", "pwa", "responsive"],
  "author": "Your Name",
  "license": "MIT"
}