Update my details pages with production-ready frontend details page (details.html, details.css, details.js) that consumes real-time data from a Flask backend. The implementation must follow best practices (clean code, responsive design, modular components) from the app.py, details.py, and content.js files and include:
The index page has the content.js which uses:
// Use slug-based URL (no parameter, just the slug after ?)
window.location.href = `/content/details.html?${encodeURIComponent(slug)}`;
Updates:
•	Hero Section with real poster, backdrop, title, genres, ratings, language, release date.
•	Action Buttons:
o	🎥 Watch Trailer → plays trailer.
o	🔖 Add to Watchlist (toggle, synced with backend).
o	❤️ Add to Favorites (toggle, synced with backend).
o	🔗 Share → copy page link + social share options (WhatsApp, Twitter/X, Facebook).
•	Overview Section with real synopsis (expandable).
•	Ratings Section showing IMDb and user scores (from backend).
•	Cast & Crew carousel/grid with real names, images, and deep links (click → actor details page with bio, previous works, upcoming projects).
•	Reviews Section with real user reviews, spoiler tags, and helpful votes (paginated).
•	Content Carousels (Similar Titles, More from this Genre, Personalized Picks).
•	Gallery Section with real posters, stills, and behind-the-scenes images in a lightbox modal.
•	Trailer Modal with real trailers, adaptive quality, and custom controls.
Additional Requirements:
•	Re-design the layout with an advanced, modern grid system (CSS Grid + Flexbox) for the details page to ensure the best UI/UX.
•	Use Advanced TailwindCSS utilities (custom animations, dark mode, responsive typography, aspect-ratio, line-clamp, grids, transitions, hover/focus states, dynamic theming).
•	Use Advanced Bootstrap 5 components (offcanvas, carousels, modals, tooltips, responsive containers, grids, utilities).
•	Ensure seamless integration of Tailwind + Bootstrap while avoiding conflicts (namespace where required).
•	Prioritize readability, spacing, and visual hierarchy in grid arrangements.
•	Make the design engaging, cinematic, and intuitive (like Netflix/Disney+).
•	Ensure all sections align seamlessly in the grid across devices.
•	No placeholders — must use only real-time API data.
•	Fully responsive (desktop, tablet, mobile).